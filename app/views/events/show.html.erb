<p id="notice"><%= notice %></p>

<% @events.each do |event| %>
<h2 class="title is-5"><%= link_to event.title, event %></h2>
<small><%= link_to event.host.username, user_path(event.host) %></small>
 <% end %>

<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
